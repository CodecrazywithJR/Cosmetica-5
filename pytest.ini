[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
pythonpath = apps/api
addopts = -q --reuse-db
testpaths = tests
markers =
    critical: Critical data integrity tests that must always pass
